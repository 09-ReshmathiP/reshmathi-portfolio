# Cabinet Grotesk

Drop the licensed `CabinetGrotesk-Variable.woff2` file in this folder
(get it free for personal use from fontshare.com/fonts/cabinet-grotesk).
Until it's added, browsers fall back to "General Sans" / system sans-serif
automatically — the site still works, just with the fallback display face.
